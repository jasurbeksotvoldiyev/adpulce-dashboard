name: Bot 1 — soatlik hisobot

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch: {}

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Hisobotni chaqirish
        run: |
          curl -sf "https://adpulce-dashboard.vercel.app/api/bot-team-report?secret=${{ secrets.CRON_SECRET }}"
